import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginBottom: 20,
  },

  title: {
    fontSize: 19,
    fontWeight: "900",
    color: "#242424",
    marginBottom: 5,
  },

  subtitle: {
    fontSize: 12,
    lineHeight: 19,
    color: "#707080",
  },
});

export default styles;
