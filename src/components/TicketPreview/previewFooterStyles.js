import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  footerText: {
    textAlign: "center",
    fontSize: 10,
    color: "#9A99A5",
    marginTop: 18,
  },
});

export default styles;
